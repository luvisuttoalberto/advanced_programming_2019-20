#include "src2.h"
#include "class.h"
void dummy2() {
  A a; 
  //differently than with circle_counter, now we don't have problems; 
  //you can repeat class/struct declaration in many source files and the compiler doesn't complain
}
