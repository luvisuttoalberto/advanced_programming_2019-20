#include "src2.h"
#include "class.h"
void dummy2() {
  A a; //differently than with ircle_counter, now we don't have problems; you an repeat lass/strut declaration in many ... and the ompiler doesn't omplain
}
